{
  "name": "clipped-page",
  "version": "0.0.0",
  "private": true,
  "description": "Chrome extension + Cloudflare-hosted site that clips an X post into a self-contained URL.",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
